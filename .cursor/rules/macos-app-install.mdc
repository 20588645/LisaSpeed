---
description: Never auto-quit/rebuild/install LisaSpeed; user runs desktop scripts
alwaysApply: true
---

# LisaSpeed macOS 安装与运行约定

本机正在使用的是 `/Applications/LisaSpeed.app`。开发时默认**只改代码、不碰正在运行的应用**。

## 禁止（除非用户明确要求）

- `killall LisaSpeed` / `killall Hiddify` 或结束相关进程
- `flutter build macos`、`make macos-release` 等全量编译后**自动**覆盖 `/Applications`
- `ditto` / `cp` 安装到 `/Applications/LisaSpeed.app`
- `open -a LisaSpeed` 在未获用户同意时强行启动/重启应用
- 为「方便验收」擅自中断用户当前会话中的代理连接

## 正确做法

1. 完成代码修改后，优先告知可用应用内更新：设置 → 关于 → **立即更新**（弹窗实时日志；会同步重编 UI + `HiddifyCli`/隧道，安装阶段自动退出旧进程并重开）。
2. 若当前安装包尚无该按钮，可双击桌面脚本完成一次构建与安装：
 - `~/Desktop/重新编译安装LisaSpeed.command`
 - 仓库副本：`scripts/rebuild-and-install.command`（必须含 `make macos-arm64-cli`，禁止只编 Flutter 却漏掉 core）
3. 断网应急（与编译无关）：
 - `~/Desktop/恢复LisaSpeed网络.command`
 - 仓库副本：`scripts/fix-network.command`

## 允许的只读排查

- 查看日志、路由、进程、配置文件（不修改、不杀进程）
- 在用户**明确说**「帮我编译/安装/重启」之后，才可执行对应操作
